apiVersion: v1
kind: Service
metadata:
  name: {{ include "oga-app.fullname" . }}
  labels:
    {{- include "oga-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "oga-app.selectorLabels" . | nindent 4 }}
